# modelsrv-web-ui-server
A server for the EmELand web UI. It provides a authentication and authorization model to allows exposing the UI beyond the closed (virtual) management network that is usually needed for the rest of the modelsrv element.
